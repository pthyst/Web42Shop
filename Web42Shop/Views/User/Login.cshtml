@model Web42Shop.ViewModels.UserLoginViewModel
@{
    ViewBag.Title = "Đăng nhập";
    Layout = "~/Views/Shared/Users/_IndexLayout.cshtml";
}

<div class="container-fluid section">
    <div class="container">
        <div class="row">
            <div class="col"></div>
            <div class="col">
                <br />
                <br />
                <form asp-controller="User" asp-action="Login" method="post">
                    <div class="form-group">
                        <label asp-for="@Model.Username">Email hoặc số điện thoại</label>
                        <input type="text" class="form-control" asp-for="@Model.Username">
                        <span asp-validation-for="@Model.Username" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="@Model.Password">Mật khẩu</label>
                        <input type="password" class="form-control" asp-for="@Model.Password">
                        <span asp-validation-for="@Model.Password" class="text-danger"></span>
                    </div>
                    
                    <br />
                    <div class="form-row">
                        <button class="button button--submit" type="submit">Đăng nhập</button>
                        <a asp-controller="User" asp-action="Register" class="button button--new">Đăng ký</a>
                    </div>
                </form>
            </div>
            <div class="col"></div>
        </div>
    </div>
</div>

